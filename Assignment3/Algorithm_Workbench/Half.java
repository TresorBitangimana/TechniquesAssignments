package Assignment3.Algorithm_Workbench;

public class Half implements Computable {

    Computable compute = (x) -> x/2;

    @Override
    public double compute(double x) {
        return compute(x);
    }
    
}
